# This file must be used with "source bin/activate" *from bash*
# you cannot run it directly

deactivate () ***REMOVED***
    unset -f pydoc >/dev/null 2>&1

    # reset old environment variables
    # ! [ -z $***REMOVED***VAR+_***REMOVED*** ] returns true if VAR is declared at all
    if ! [ -z "$***REMOVED***_OLD_VIRTUAL_PATH+_***REMOVED***" ] ; then
        PATH="$_OLD_VIRTUAL_PATH"
        export PATH
        unset _OLD_VIRTUAL_PATH
    fi
    if ! [ -z "$***REMOVED***_OLD_VIRTUAL_PYTHONHOME+_***REMOVED***" ] ; then
        PYTHONHOME="$_OLD_VIRTUAL_PYTHONHOME"
        export PYTHONHOME
        unset _OLD_VIRTUAL_PYTHONHOME
    fi

    # This should detect bash and zsh, which have a hash command that must
    # be called to get it to forget past commands.  Without forgetting
    # past commands the $PATH changes we made may not be respected
    if [ -n "$***REMOVED***BASH-***REMOVED***" ] || [ -n "$***REMOVED***ZSH_VERSION-***REMOVED***" ] ; then
        hash -r 2>/dev/null
    fi

    if ! [ -z "$***REMOVED***_OLD_VIRTUAL_PS1+_***REMOVED***" ] ; then
        PS1="$_OLD_VIRTUAL_PS1"
        export PS1
        unset _OLD_VIRTUAL_PS1
    fi

    unset VIRTUAL_ENV
    if [ ! "$***REMOVED***1-***REMOVED***" = "nondestructive" ] ; then
    # Self destruct!
        unset -f deactivate
    fi
***REMOVED***

# unset irrelevant variables
deactivate nondestructive

VIRTUAL_ENV="/Users/mucahiderenler/Desktop/wololoDjango/DjangoFirebaseProject/django"
export VIRTUAL_ENV

_OLD_VIRTUAL_PATH="$PATH"
PATH="$VIRTUAL_ENV/bin:$PATH"
export PATH

# unset PYTHONHOME if set
if ! [ -z "$***REMOVED***PYTHONHOME+_***REMOVED***" ] ; then
    _OLD_VIRTUAL_PYTHONHOME="$PYTHONHOME"
    unset PYTHONHOME
fi

if [ -z "$***REMOVED***VIRTUAL_ENV_DISABLE_PROMPT-***REMOVED***" ] ; then
    _OLD_VIRTUAL_PS1="$***REMOVED***PS1-***REMOVED***"
    if [ "x" != x ] ; then
        PS1="$***REMOVED***PS1-***REMOVED***"
    else
        PS1="(`basename \"$VIRTUAL_ENV\"`) $***REMOVED***PS1-***REMOVED***"
    fi
    export PS1
fi

# Make sure to unalias pydoc if it's already there
alias pydoc 2>/dev/null >/dev/null && unalias pydoc || true

pydoc () ***REMOVED***
    python -m pydoc "$@"
***REMOVED***

# This should detect bash and zsh, which have a hash command that must
# be called to get it to forget past commands.  Without forgetting
# past commands the $PATH changes we made may not be respected
if [ -n "$***REMOVED***BASH-***REMOVED***" ] || [ -n "$***REMOVED***ZSH_VERSION-***REMOVED***" ] ; then
    hash -r 2>/dev/null
fi
